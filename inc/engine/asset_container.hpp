#pragma once

#include <vector>

namespace engine {

template<typename T> class AssetContainer {
    std::vector<T> assets;
    std::vector<unsigned int> indices; 
    std::vector<
};




} // namespace engine
